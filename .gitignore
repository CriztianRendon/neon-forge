# Credenciales y variables de entorno (nunca subir)
.env
.env.*

# Vercel CLI (por si algun dia se despliega desde la terminal)
.vercel

# Archivos generados por el sistema operativo
Thumbs.db
desktop.ini
.DS_Store

# Carpetas de dependencias (este proyecto no usa, pero por seguridad)
node_modules/
